# Copy this file to `.env` (same folder as docker-compose.yml) and set a
# real secret before running anything beyond local/dev testing.
# docker-compose automatically reads a `.env` file in this directory.
JWT_SECRET_KEY=change-this-to-a-long-random-string
GEMINI_API_KEY=your-gemini-api-key
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-javascript-api-key
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=younga.aime@ictuniversity.edu.cm
SMTP_PASSWORD=kpbivpjlaegmzttd
SMTP_FROM=younga.aime@ictuniversity.edu.cm